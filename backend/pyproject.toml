[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kaiwen", email = "ngkaiwen@footfallcam.com" }
]
requires-python = ">=3.12"
dependencies = [
    "datetime>=6.0",
    "flask>=3.1.3",
    "psycopg>=3.3.5",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
